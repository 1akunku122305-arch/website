# /etc/fail2ban/filter.d/wangstore-probe.conf
# Bans vulnerability scanners probing for non-existent admin surfaces.
[Definition]
failregex = ^<HOST> .* "(GET|POST|HEAD) [^"]*(wp-admin|wp-login|xmlrpc\.php|\.env|\.git|phpmyadmin|/admin\.php|/shell)[^"]*" (403|404|405) 
ignoreregex =
