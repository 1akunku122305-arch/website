# /etc/fail2ban/filter.d/wangstore-login.conf
# Bans repeated failed or rate-limited authentication attempts.
[Definition]
failregex = ^<HOST> .* "POST /api/auth/login HTTP/[0-9.]+" (401|429) 
ignoreregex =
